name: Build & Push To Registry

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  COMPOSE_FILE: "docker-compose.prod.yml"
  REGISTRY_URL: "192.168.1.7:5000"
  REGISTRY_USERNAME: "admin"
  REGISTRY_PASSWORD: "CyMa2005!"

jobs:
  build-and-push:
    runs-on: self-hosted
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      # --- FIX START ---
      # Wir zwingen Docker, den 'docker'-Treiber (Host-Daemon) zu nutzen.
      # Das verhindert den Versuch, 'fuse-overlayfs' zu verwenden.
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: docker
      # --- FIX ENDE ---
      
      - name: Generate Dynamic Configuration
        run: |
          RAW_NAME="${{ github.event.repository.name }}"
          LOWERCASE_NAME=$(echo "$RAW_NAME" | tr '[:upper:]' '[:lower:]')
          
          echo "APP_NAME=$LOWERCASE_NAME" > .env
          echo "REGISTRY_URL=${{ env.REGISTRY_URL }}" >> .env
          
          echo "✅ Configured for project: $LOWERCASE_NAME"

      - name: Log In To Registry
        run: |
          echo "🔐 Authenticating against ${{ env.REGISTRY_URL }}"
          echo "${{ env.REGISTRY_PASSWORD }}" | docker login ${{ env.REGISTRY_URL }} -u "${{ env.REGISTRY_USERNAME }}" --password-stdin

      - name: Build Images
        run: |
          echo "🏗️ Building services..."
          # Der Build nutzt nun automatisch den oben konfigurierten Buildx-Treiber
          docker compose -f ${{ env.COMPOSE_FILE }} build --no-cache

      - name: Push to Registry
        run: |
          echo "⬆️ Uploading images..."
          docker compose -f ${{ env.COMPOSE_FILE }} push